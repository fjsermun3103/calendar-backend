## Manual para abrir el proyecto por primera vez (dev)

1. Instalar las dependecias `npm install`
2. Clonar el archivo `.env.template` y renombrarlo a `.env`
3. Cambiar las variables de entorno según tu configuración
4. Iniciar el servidor `npm start` o `npm run dev`